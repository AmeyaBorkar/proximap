# apps/web (planned)

The proximap Web UI — a map-based front end for searching nearby amenities.

**Not yet implemented.** This directory reserves the location in the monorepo;
see the [roadmap](../../ROADMAP.md). It will consume `@proximap/core` and reuse
the same provider abstraction as the CLI and MCP server.
